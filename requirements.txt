# Core MCP + browser
mcp[cli]>=1.2.0
playwright>=1.45.0

# Error tracking
sentry-sdk>=2.0.0

# PDF reading
pypdf>=4.0.0

# Scheduler (for schedule_task tool)
apscheduler>=3.10.0

# Windows notifications (optional, Windows only)
# winotify>=1.1.0
